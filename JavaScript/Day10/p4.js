function add(num1, num2) {
  return num1 + num2;
}
// In JS , the return statement stops the execution of a function and sends a specific value back to the function caller
let result = add(3, 5);
console.log(result);
